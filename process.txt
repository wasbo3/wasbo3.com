# --- START & SAVE DEV SESSION ---
git checkout dev
git pull origin dev

# Save work
git add .
git commit -m "your message"
git push origin dev

# --- PUSH DEV DIRECTLY TO MAIN (Without switching branches!) ---
git push origin dev:main


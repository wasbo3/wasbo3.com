# --- 1. START SESSION ---
git checkout dev && git pull origin dev

# --- 2. SAVE & PUSH EVERYTHING AT ONCE ---
git commit -am "your message" && git push origin dev && git push origin dev:main --force